using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BodyDamage : MonoBehaviour
{

    public string _bodypartName;
    public bool _hasInstaKill = false;
    public float _damageMultiplier = 1.0f;
    public Transform _Owner;
    public float bodyPartHealth = 0f;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
